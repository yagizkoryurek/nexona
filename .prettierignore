node_modules
.next
out
build
pnpm-lock.yaml
next-env.d.ts

# Self-contained Expo app — formatted by its own toolchain, not the web
# app's Prettier config. Without this, `pnpm format` rewrites its files.
mobile
